# ═══════════════════════════════════════════════════════════════════════════
# SamarthX Superset - Production Requirements
# ═══════════════════════════════════════════════════════════════════════════
# Versions match what's running locally (verified working)
# DO NOT change these versions - they match the encrypted database state
# ═══════════════════════════════════════════════════════════════════════════

# Core Superset (CRITICAL: must match version that encrypted the database)
apache-superset==6.0.0

# Database driver
psycopg2-binary==2.9.11

# Production WSGI server
gunicorn==23.0.0

# Encryption library (used for database password encryption)
cryptography==44.0.3

# Environment variable loader
python-dotenv==1.2.2

# Image processing (for branding/logos)
Pillow==11.3.0

# JSON handling (used by Superset internally)
simplejson==3.20.2
